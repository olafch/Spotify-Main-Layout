import { View, StyleSheet } from "react-native";

export const UpperMenuIcon = () => {
  return <View style={styles.upperMenuIcon} />;
};

const styles = StyleSheet.create({
  upperMenuIcon: {
    height: 36,
    width: 36,
    borderRadius: 18,
    backgroundColor: "#fcfcfcff",
  },
});
